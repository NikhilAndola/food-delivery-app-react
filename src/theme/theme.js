import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        main: '#ff0000',
      },
      seondary: {
        main: 'rgb(201, 71, 98)',
      },
    },
  });

export default theme;